# Sensor-Aware Adaptive Linear Trajectory Model

A sensor-aware adaptive extension of LGCP-based barrier coverage, where targets remain locally linear but their trajectory intensity is reweighted toward low-detection paths conditioned on the deployed sensor configuration.